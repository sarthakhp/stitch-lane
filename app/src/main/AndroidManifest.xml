<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StitchLane"
        tools:targetApi="31">

        <activity
            android:name=".activity.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activity.AddNewCustomerActivity"
            android:label="Save New Customer">
            <!-- Optionally, add intent-filter if this activity should be accessible from other components -->
        </activity>

        <activity
            android:name=".activity.CustomerInfoActivity"
            android:label="Customer Details">
            <!-- Optionally, add intent-filter if this activity should be accessible from other components -->
        </activity>

        <activity
            android:name=".activity.NewOrderActivity"
            android:label="Customer Details">
            <!-- Optionally, add intent-filter if this activity should be accessible from other components -->
        </activity>


    </application>

    <uses-permission android:name="android.permission.READ_CONTACTS" />


</manifest>